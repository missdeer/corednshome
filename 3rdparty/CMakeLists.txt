cmake_minimum_required(VERSION 3.15)
project(3rdparty VERSION 1.0.0)

IF(APPLE)
    add_subdirectory (zlib)
ELSE()
    find_package(ZLIB)
    IF(NOT ZLIB_FOUND)
        add_subdirectory (zlib)
    ENDIF()
ENDIF()
